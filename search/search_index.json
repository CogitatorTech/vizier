{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Vizier","text":"<p>A physical design advisor for DuckDB.</p> <p>Vizier is a DuckDB extension that analyzes your (query) workload and recommends physical design changes like indexes, sort orders, Parquet layouts, and summary tables to improve query performance.</p>"},{"location":"#why-vizier","title":"Why Vizier?","text":"<p>When you have a DuckDB database, you are typically on your own to figure out things like:</p> <ul> <li>Should I rewrite my table sorted differently?</li> <li>What sort order should my Parquet exports use?</li> <li>Are any of my indexes redundant?</li> <li>Which queries are my bottleneck?</li> </ul> <p>There are tools like pg_qualstats (for PostgreSQL) and Database Engine Tuning Advisor (for SQL Server) that try to solve these kinds of problems, but nothing equivalent exists for DuckDB. Vizier is a step towards filling that gap.</p>"},{"location":"#how-it-works","title":"How It Works?","text":"<p>You feed Vizier your workload (a collection of queries), and it tells you what to change in your database to make it run faster.</p> <p>Install Vizier from the hosted DuckDB extension repository, then use the workflow below.</p> <pre><code>install vizier from 'https://cogitatortech.github.io/vizier/extensions';\nload vizier;\n\n-- Capture your real queries\nselect * from vizier_capture('select * from events where account_id = 42 and ts &gt;= date ''2026-01-01''');\nselect * from vizier_flush();\n\n-- Get recommendations\nselect * from vizier_analyze();\nselect * from vizier.recommendations;\n-- Create index idx_events_account_id on events(account_id)\n-- Rewrite events sorted by (account_id, ts) for scan pruning\n\n-- Apply and measure the impact\nselect * from vizier_apply(1);\nselect * from vizier_benchmark('select * from events where account_id = 42', 10);\n</code></pre>"},{"location":"#when-to-use-vizier","title":"When to Use Vizier?","text":"<p>Vizier is most useful for:</p> <ul> <li>Analyzing your query patterns and recommends the sort order and partitioning strategy for Parquet exports. Getting the   sort order wrong means 10-100x worse row-group pruning.</li> <li>Optimizing persistent DuckDB tables. If you have tables that get scanned repeatedly with the same filter patterns, Vizier identifies which columns   to sort by for scan pruning.</li> <li>Understanding workload patterns. <code>vizier.workload_summary</code>, <code>vizier.inspect_table()</code>, and <code>vizier.overview()</code> give you a quick picture of which   tables and columns are under the most pressure. This can be useful when inheriting a database you did not build.</li> </ul> <p>Vizier is less useful for:</p> <ul> <li>Ad-hoc notebook analysis (no repeating patterns)</li> <li>Tiny datasets (DuckDB is already fast).</li> <li>Read-only Parquet scans like reading Parquet files on an S3 bucket.</li> </ul>"},{"location":"#what-vizier-is-not","title":"What Vizier Is Not","text":"<p>Vizier is not an auto-tuner. It does not rewrite your tables for you. It recommends changes, explains why, lets you dry-run, and measures the impact. So you make the decision to apply at the end.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Fully cross-platform (Windows, Linux, macOS, and FreeBSD)</li> <li>Supports DuckDB version <code>1.2.0</code> or newer</li> <li>Query pattern and execution stat capture</li> <li>Physical design inspection (indexes, table sizes, cardinalities, and storage layout)</li> <li>Workload analysis with bottleneck detection</li> <li>Index, sort order, Parquet layout, and summary table recommendations</li> <li>Dry-run support and before/after benchmarking</li> <li>Rollback for applied recommendations</li> <li>Persistent state across sessions</li> <li>Static HTML report generation</li> </ul>"},{"location":"#table-of-contents","title":"Table of Contents","text":"<ul> <li>Getting Started</li> <li>Examples</li> <li>API Reference</li> <li>Current Limitations</li> </ul>"},{"location":"api-reference/","title":"API Reference","text":""},{"location":"api-reference/#scalar-functions","title":"Scalar Functions","text":""},{"location":"api-reference/#vizier_version","title":"<code>vizier_version()</code>","text":"<p>Returns the Vizier extension version string.</p> <pre><code>select vizier_version();\n</code></pre>"},{"location":"api-reference/#vizier_configurekey-value","title":"<code>vizier_configure(key, value)</code>","text":"<p>Updates a setting in the <code>vizier.settings</code> table. Returns the new value.</p> <pre><code>select vizier_configure('benchmark_runs', '10');\n</code></pre>"},{"location":"api-reference/#vizier_session_logsql","title":"<code>vizier_session_log(sql)</code>","text":"<p>Logs a query to the active capture session. Used between <code>vizier_start_capture()</code> and <code>vizier_stop_capture()</code>.</p> <pre><code>select vizier_session_log('select * from events where id = 1');\n</code></pre>"},{"location":"api-reference/#table-functions","title":"Table Functions","text":""},{"location":"api-reference/#vizier_initpath","title":"<code>vizier_init(path)</code>","text":"<p>Initializes the Vizier schema and metadata tables. If <code>path</code> is provided, sets the persistent state file.</p> <pre><code>select * from vizier_init('/path/to/state.db');\n</code></pre>"},{"location":"api-reference/#vizier_capturesql","title":"<code>vizier_capture(sql)</code>","text":"<p>Captures a single query for workload analysis. The query is normalized (literals replaced with <code>?</code>, keywords uppercased, and whitespace collapsed) and deduplicated by signature.</p> <pre><code>select * from vizier_capture('select * from events where account_id = 42');\n</code></pre>"},{"location":"api-reference/#vizier_capture_bulktable-column","title":"<code>vizier_capture_bulk(table, column)</code>","text":"<p>Reads SQL statements from a table column and captures them.</p> <pre><code>select * from vizier_capture_bulk('query_log', 'sql_text');\n</code></pre>"},{"location":"api-reference/#vizier_start_capture","title":"<code>vizier_start_capture()</code>","text":"<p>Starts session-based capture. Queries logged via <code>vizier_session_log()</code> are buffered until <code>vizier_stop_capture()</code> is called.</p> <pre><code>select * from vizier_start_capture();\n</code></pre>"},{"location":"api-reference/#vizier_stop_capture","title":"<code>vizier_stop_capture()</code>","text":"<p>Stops session capture, imports logged queries, and flushes them to metadata tables.</p> <pre><code>select * from vizier_stop_capture();\n</code></pre>"},{"location":"api-reference/#vizier_import_profilepath","title":"<code>vizier_import_profile(path)</code>","text":"<p>Imports queries from a DuckDB JSON profiling output file.</p> <pre><code>select * from vizier_import_profile('/path/to/profile.json');\n</code></pre>"},{"location":"api-reference/#vizier_flush","title":"<code>vizier_flush()</code>","text":"<p>Persists all captured queries from the in-memory buffer to the <code>vizier.workload_queries</code> and <code>vizier.workload_predicates</code> tables. Also populates <code>tables_json</code>, <code>columns_json</code>, and selectivity data.</p> <pre><code>select * from vizier_flush();\n</code></pre>"},{"location":"api-reference/#vizier_analyze","title":"<code>vizier_analyze()</code>","text":"<p>Runs all advisors on the captured workload and populates <code>vizier.recommendation_store</code>. The advisors are:</p> Advisor Recommendation kind Description Index <code>create_index</code> ART indexes for selective equality and join predicates Sort <code>rewrite_sorted_table</code> Table rewrites with better row order for scan pruning Redundant index <code>drop_redundant_index</code> Indexes that are a prefix of another index Parquet sort <code>parquet_sort_order</code> Sort order for Parquet exports Parquet partition <code>parquet_partition</code> Partitioning strategy for Parquet exports Parquet row group <code>parquet_row_group_size</code> Row group size for Parquet exports Summary table <code>create_summary_table</code> Precomputed rollup tables for repeated aggregations Join path <code>sort_for_join</code> Sort by join key for merge-join compatibility No action <code>no_action</code> Tables in the workload with no pending recommendations <pre><code>select * from vizier_analyze();\n</code></pre>"},{"location":"api-reference/#vizier_applyid-dry_run","title":"<code>vizier_apply(id, dry_run)</code>","text":"<p>Executes a recommendation from <code>vizier.recommendation_store</code>. With <code>dry_run =&gt; true</code>, shows the SQL without executing.</p> <pre><code>select * from vizier_apply(1);\nselect * from vizier_apply(1, dry_run =&gt; true);\n</code></pre>"},{"location":"api-reference/#vizier_apply_allmin_score-max_actions-dry_run","title":"<code>vizier_apply_all(min_score, max_actions, dry_run)</code>","text":"<p>Applies all pending recommendations above a score threshold.</p> Parameter Type Description <code>min_score</code> double Minimum score (0 to 1) <code>max_actions</code> bigint Maximum recommendations to apply <code>dry_run</code> boolean Preview without executing <pre><code>select * from vizier_apply_all(min_score =&gt; 0.7, max_actions =&gt; 5, dry_run =&gt; false);\n</code></pre>"},{"location":"api-reference/#vizier_rollbackaction_id","title":"<code>vizier_rollback(action_id)</code>","text":"<p>Reverses a single applied recommendation by executing its stored inverse SQL.</p> <pre><code>select * from vizier_rollback(1);\n</code></pre>"},{"location":"api-reference/#vizier_rollback_all","title":"<code>vizier_rollback_all()</code>","text":"<p>Undoes all applied recommendations in reverse order.</p> <pre><code>select * from vizier_rollback_all();\n</code></pre>"},{"location":"api-reference/#vizier_benchmarksql-runs","title":"<code>vizier_benchmark(sql, runs)</code>","text":"<p>Runs a query multiple times and returns timing statistics.</p> <pre><code>select * from vizier_benchmark('select count(*) from events', 10);\n</code></pre>"},{"location":"api-reference/#vizier_compareid","title":"<code>vizier_compare(id)</code>","text":"<p>Benchmarks a recommendation's target queries before and after applying. Captures <code>EXPLAIN</code> plans for both.</p> <pre><code>select * from vizier_compare(1);\n</code></pre>"},{"location":"api-reference/#vizier_replaytable_name","title":"<code>vizier_replay(table_name)</code>","text":"<p>Re-runs all captured queries and records timing in <code>vizier.replay_results</code>. Optionally filtered by table.</p> <pre><code>select * from vizier_replay();\nselect * from vizier_replay(table_name =&gt; 'events');\n</code></pre>"},{"location":"api-reference/#vizier_savepath","title":"<code>vizier_save(path)</code>","text":"<p>Saves all Vizier state to a DuckDB file.</p> <pre><code>select * from vizier_save('/tmp/vizier_state.db');\n</code></pre>"},{"location":"api-reference/#vizier_loadpath","title":"<code>vizier_load(path)</code>","text":"<p>Loads Vizier state from a previously saved file.</p> <pre><code>select * from vizier_load('/tmp/vizier_state.db');\n</code></pre>"},{"location":"api-reference/#vizier_reportpath","title":"<code>vizier_report(path)</code>","text":"<p>Generates a standalone HTML report.</p> <pre><code>select * from vizier_report('/tmp/report.html');\n</code></pre>"},{"location":"api-reference/#vizier_dashboardpath","title":"<code>vizier_dashboard(path)</code>","text":"<p>Generates an interactive HTML dashboard.</p> <pre><code>select * from vizier_dashboard('/tmp/dashboard.html');\n</code></pre>"},{"location":"api-reference/#views","title":"Views","text":""},{"location":"api-reference/#vizierrecommendations","title":"<code>vizier.recommendations</code>","text":"<p>Pending recommendations ranked by score.</p> Column Type Description <code>recommendation_id</code> integer Unique ID <code>kind</code> varchar Recommendation type <code>table_name</code> varchar Target table <code>columns_json</code> varchar Affected columns (JSON) <code>score</code> double Recommendation score (0 to 1) <code>confidence</code> double Confidence level (0 to 1) <code>reason</code> varchar Why this was recommended <code>sql_text</code> varchar SQL to execute <code>status</code> varchar pending, applied, or rolled_back <code>estimated_gain</code> varchar Estimated performance improvement <code>estimated_build_cost</code> varchar Cost to build (like index creation time) <code>estimated_maintenance_cost</code> varchar Ongoing cost (like write overhead)"},{"location":"api-reference/#vizierworkload_summary","title":"<code>vizier.workload_summary</code>","text":"<p>Summary of captured queries ordered by execution count.</p> Column Type Description <code>sig</code> varchar Query signature hash <code>sql</code> varchar Sample SQL <code>runs</code> integer Execution count <code>last_seen</code> varchar Last capture timestamp <code>avg_ms</code> double Average execution time"},{"location":"api-reference/#vizierchange_history","title":"<code>vizier.change_history</code>","text":"<p>Applied changes with rollback availability.</p> Column Type Description <code>action_id</code> integer Action ID <code>recommendation_id</code> integer Source recommendation <code>kind</code> varchar Recommendation type <code>table_name</code> varchar Target table <code>sql_text</code> varchar SQL that was executed <code>inverse_sql</code> varchar SQL to undo <code>success</code> boolean Whether execution succeeded <code>applied_at</code> varchar Timestamp <code>can_rollback</code> boolean Whether rollback is available"},{"location":"api-reference/#vizierreplay_summary","title":"<code>vizier.replay_summary</code>","text":"<p>Replay results with regression detection.</p> Column Type Description <code>query_signature</code> varchar Query signature hash <code>sample_sql</code> varchar Sample SQL <code>avg_ms</code> double Replay average time <code>status</code> varchar Execution status <code>verdict</code> varchar improved, stable, or regression"},{"location":"api-reference/#vizierreplay_totals","title":"<code>vizier.replay_totals</code>","text":"<p>Aggregate replay metrics.</p> Column Type Description <code>queries_replayed</code> integer Number of queries replayed <code>replay_total_ms</code> double Total replay time <code>baseline_total_ms</code> double Total baseline time <code>ratio</code> double Replay time / baseline time <code>overall_verdict</code> varchar improved, stable, or regression"},{"location":"api-reference/#table-macros","title":"Table Macros","text":""},{"location":"api-reference/#vizierinspect_tablename","title":"<code>vizier.inspect_table(name)</code>","text":"<p>Per-column view of a table showing type, nullability, size, index count, and predicate usage from the captured workload.</p> <pre><code>select * from vizier.inspect_table('events');\n</code></pre>"},{"location":"api-reference/#vizieroverview","title":"<code>vizier.overview()</code>","text":"<p>All user tables with sizes, index counts, and predicate hotspots.</p> <pre><code>select * from vizier.overview();\n</code></pre>"},{"location":"api-reference/#vizierexplainid","title":"<code>vizier.explain(id)</code>","text":"<p>Full details of a single recommendation.</p> <pre><code>select * from vizier.explain(1);\n</code></pre>"},{"location":"api-reference/#vizierexplain_humanid","title":"<code>vizier.explain_human(id)</code>","text":"<p>Natural language explanation with estimated speedup and write overhead.</p> <pre><code>select * from vizier.explain_human(1);\n</code></pre>"},{"location":"api-reference/#vizierscore_breakdownid","title":"<code>vizier.score_breakdown(id)</code>","text":"<p>Per-predicate scoring breakdown showing how each column contributes to the score.</p> <pre><code>select * from vizier.score_breakdown(1);\n</code></pre>"},{"location":"api-reference/#viziercompareid","title":"<code>vizier.compare(id)</code>","text":"<p>Before/after benchmark comparison with verdict (excellent, good, marginal, or regression).</p> <pre><code>select * from vizier.compare(1);\n</code></pre>"},{"location":"api-reference/#vizieranalyze_tablename","title":"<code>vizier.analyze_table(name)</code>","text":"<p>Pending recommendations are filtered to a single table.</p> <pre><code>select * from vizier.analyze_table('events');\n</code></pre>"},{"location":"api-reference/#vizieranalyze_workloadsince-min_executions","title":"<code>vizier.analyze_workload(since, min_executions)</code>","text":"<p>Captured queries filtered by time and minimum execution count.</p> <pre><code>select * from vizier.analyze_workload('2026-01-01', 5);\n</code></pre>"},{"location":"api-reference/#metadata-tables","title":"Metadata Tables","text":""},{"location":"api-reference/#vizierworkload_queries","title":"<code>vizier.workload_queries</code>","text":"<p>Captured query patterns with execution statistics.</p> Column Type Description <code>query_signature</code> varchar Normalized query hash <code>normalized_sql</code> varchar Normalized SQL (literals replaced with <code>?</code>) <code>sample_sql</code> varchar Original SQL sample <code>first_seen</code> varchar First capture timestamp <code>last_seen</code> varchar Last capture timestamp <code>execution_count</code> integer Number of times captured <code>total_time_ms</code> double Total execution time <code>avg_time_ms</code> double Average execution time <code>p95_time_ms</code> double 95th percentile execution time <code>tables_json</code> varchar Referenced tables (JSON) <code>columns_json</code> varchar Referenced columns (JSON) <code>estimated_rows</code> bigint Estimated rows from <code>EXPLAIN</code> (0 if unavailable)"},{"location":"api-reference/#vizierworkload_predicates","title":"<code>vizier.workload_predicates</code>","text":"<p>Predicates extracted from captured queries.</p> Column Type Description <code>query_signature</code> varchar Source query signature <code>table_name</code> varchar Table referenced <code>column_name</code> varchar Column referenced <code>predicate_kind</code> varchar equality, range, in_list, like, or group_by <code>frequency</code> integer How often this predicate appears <code>avg_selectivity</code> double Estimated selectivity (0 to 1)"},{"location":"api-reference/#vizierrecommendation_store","title":"<code>vizier.recommendation_store</code>","text":"<p>All generated recommendations. Same columns as the <code>vizier.recommendations</code> view, but includes all statuses (not just pending).</p>"},{"location":"api-reference/#vizierapplied_actions","title":"<code>vizier.applied_actions</code>","text":"<p>Audit log of applied recommendations.</p> Column Type Description <code>action_id</code> integer Unique action ID <code>recommendation_id</code> integer Source recommendation <code>applied_at</code> varchar Timestamp <code>sql_text</code> varchar SQL that was executed <code>inverse_sql</code> varchar SQL to undo <code>success</code> boolean Whether execution succeeded <code>notes</code> varchar Status notes"},{"location":"api-reference/#vizierbenchmark_results","title":"<code>vizier.benchmark_results</code>","text":"<p>Benchmark comparison results.</p> Column Type Description <code>benchmark_id</code> integer Unique benchmark ID <code>recommendation_id</code> integer Source recommendation <code>query_signature</code> varchar Query tested <code>baseline_ms</code> double Baseline timing <code>candidate_ms</code> double Post-change timing <code>speedup</code> double baseline_ms / candidate_ms <code>plan_before</code> varchar <code>EXPLAIN</code> plan before <code>plan_after</code> varchar <code>EXPLAIN</code> plan after <code>recorded_at</code> varchar Timestamp"},{"location":"api-reference/#vizierreplay_results","title":"<code>vizier.replay_results</code>","text":"<p>Per-query replay results.</p> Column Type Description <code>replay_id</code> integer Unique replay ID <code>query_signature</code> varchar Query signature <code>sample_sql</code> varchar Sample SQL <code>avg_ms</code> double Replay average time <code>status</code> varchar Execution status <code>replayed_at</code> varchar Timestamp"},{"location":"api-reference/#viziersession_log","title":"<code>vizier.session_log</code>","text":"<p>Queries captured during a session.</p> Column Type Description <code>sql_text</code> varchar Captured SQL <code>captured_at</code> varchar Timestamp"},{"location":"api-reference/#settings","title":"Settings","text":"<p>Default configuration values in <code>vizier.settings</code>:</p> Key Default Description <code>min_query_count</code> 1 Minimum query frequency before generating recommendations <code>min_confidence</code> 0.4 Minimum confidence threshold <code>benchmark_runs</code> 5 Default benchmark iterations <code>max_pending_captures</code> 4096 Maximum queries in capture buffer <code>auto_flush</code> false Flush after each capture <code>state_path</code> (empty) Auto-save/load state file path <code>index_score_divisor</code> 10 Index advisor score divisor <code>sort_score_divisor</code> 20 Sort advisor score divisor <code>sort_min_predicates</code> 2 Minimum predicates before sort advisor fires <code>large_table_bytes</code> 100000000 Table size threshold for maintenance cost warnings <code>compare_bench_runs</code> 5 Benchmark runs for <code>vizier_compare</code> <code>cost_weight_time</code> 1.0 Weight for query time impact in scoring <code>cost_weight_frequency</code> 1.0 Weight for query frequency in scoring <code>cost_weight_selectivity</code> 1.5 Boost for high-selectivity columns in scoring"},{"location":"examples/","title":"Examples","text":"<p>Before running the examples below, install Vizier from the hosted DuckDB extension repository and <code>load vizier;</code>.</p> <pre><code>install vizier from 'https://cogitatortech.github.io/vizier/extensions';\nload vizier;\n</code></pre>"},{"location":"examples/#index-recommendations","title":"Index Recommendations","text":"<p>Capture queries with equality filters and let Vizier detect index candidates:</p> <pre><code>-- Simulate a workload with repeated equality lookups\nselect * from vizier_capture('select * from customers where email = ''alice@example.com''');\nselect * from vizier_capture('select * from customers where email = ''bob@example.com''');\nselect * from vizier_capture('select * from customers where email = ''carol@example.com''');\nselect * from vizier_flush();\n\n-- Analyze and review\nselect * from vizier_analyze();\nselect * from vizier.recommendations;\n-- kind: create_index\n-- sql_text: create index idx_customers_email on customers(email)\n</code></pre>"},{"location":"examples/#sort-order-recommendations","title":"Sort Order Recommendations","text":"<p>When queries repeatedly filter on the same column prefixes, Vizier recommends sorting the table for row-group pruning:</p> <pre><code>select * from vizier_capture('select * from events where account_id = 1 and ts &gt;= ''2026-01-01''');\nselect * from vizier_capture('select * from events where account_id = 2 and ts &gt;= ''2026-02-01''');\nselect * from vizier_capture('select * from events where account_id = 3 and ts between ''2026-01-01'' and ''2026-06-01''');\nselect * from vizier_flush();\n\nselect * from vizier_analyze();\nselect * from vizier.recommendations;\n-- kind: rewrite_sorted_table\n-- sql_text: create table events_sorted as select * from events order by account_id, ts\n</code></pre>"},{"location":"examples/#redundant-index-detection","title":"Redundant Index Detection","text":"<p>If you have indexes where one is a prefix of another, Vizier flags the shorter one:</p> <pre><code>select * from vizier_analyze();\nselect * from vizier.recommendations where kind = 'drop_redundant_index';\n-- sql_text: drop index idx_orders_customer_id\n-- reason: prefix of idx_orders_customer_id_date\n</code></pre>"},{"location":"examples/#parquet-layout-recommendations","title":"Parquet Layout Recommendations","text":"<p>For tables exported to Parquet, Vizier recommends sort orders and partitioning:</p> <pre><code>select * from vizier_analyze();\nselect * from vizier.recommendations where kind = 'parquet_sort_order';\n-- sql_text: copy events to 'events.parquet' (format parquet, row_group_size 100000) order by account_id, ts\n\nselect * from vizier.recommendations where kind = 'parquet_partition';\n-- sql_text: copy events to 'events_partitioned' (format parquet, partition_by (region))\n</code></pre>"},{"location":"examples/#summary-table-recommendations","title":"Summary Table Recommendations","text":"<p>When the same aggregation runs repeatedly, Vizier recommends a precomputed table:</p> <pre><code>select * from vizier_capture('select customer_id, sum(amount) from orders group by customer_id');\nselect * from vizier_capture('select customer_id, sum(amount) from orders group by customer_id');\nselect * from vizier_capture('select customer_id, sum(amount) from orders group by customer_id');\nselect * from vizier_flush();\n\nselect * from vizier_analyze();\nselect * from vizier.recommendations where kind = 'create_summary_table';\n</code></pre>"},{"location":"examples/#inspecting-table-design","title":"Inspecting Table Design","text":"<p>Review the physical design of a specific table:</p> <pre><code>select * from vizier.inspect_table('events');\n</code></pre> <p>This shows each column with its type, nullability, size, index count, and predicate usage from the captured workload.</p> <p>For a database-wide overview:</p> <pre><code>select * from vizier.overview();\n</code></pre>"},{"location":"examples/#understanding-a-recommendation","title":"Understanding a Recommendation","text":"<p>Get the scoring breakdown for a recommendation:</p> <pre><code>select * from vizier.explain(1);\nselect * from vizier.score_breakdown(1);\nselect * from vizier.explain_human(1);\n</code></pre> <p><code>vizier.explain_human</code> returns a natural language explanation with estimated speedup and write overhead.</p>"},{"location":"examples/#batch-apply-with-threshold","title":"Batch Apply with Threshold","text":"<p>Apply all recommendations above a minimum score:</p> <pre><code>-- Preview what would be applied\nselect * from vizier_apply_all(min_score =&gt; 0.7, max_actions =&gt; 10, dry_run =&gt; true);\n\n-- Apply\nselect * from vizier_apply_all(min_score =&gt; 0.7, max_actions =&gt; 10, dry_run =&gt; false);\n</code></pre>"},{"location":"examples/#rollback","title":"Rollback","text":"<p>Undo a single applied recommendation:</p> <pre><code>select * from vizier_rollback(1);\n</code></pre> <p>Or undo all applied recommendations in reverse order:</p> <pre><code>select * from vizier_rollback_all();\n</code></pre> <p>Review what was applied and what can be rolled back:</p> <pre><code>select * from vizier.change_history;\n</code></pre>"},{"location":"examples/#workload-replay","title":"Workload Replay","text":"<p>After applying changes, replay the captured workload to detect regressions:</p> <pre><code>select * from vizier_replay();\nselect * from vizier.replay_summary;\nselect * from vizier.replay_totals;\n</code></pre> <p>Replay only queries that touch a specific table:</p> <pre><code>select * from vizier_replay(table_name =&gt; 'events');\n</code></pre>"},{"location":"examples/#configuration","title":"Configuration","text":"<p>View and update settings:</p> <pre><code>select * from vizier.settings;\nselect vizier_configure('benchmark_runs', '10');\nselect vizier_configure('min_confidence', '0.6');\n</code></pre>"},{"location":"examples/#html-report","title":"HTML Report","text":"<p>Generate a static HTML report of the current analysis:</p> <pre><code>select * from vizier_report('/tmp/vizier_report.html');\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#build-from-source","title":"Build from Source","text":"<p>Vizier requires Zig 0.15.2 and a DuckDB binary (version <code>1.2.0</code> or newer) for testing.</p> <pre><code>git clone --depth=1 https://github.com/CogitatorTech/vizier.git\ncd vizier\n\n# Build the extension\nmake build-all\n\n# Run tests (optional; needs DuckDB in PATH)\nmake test\n\n# Try Vizier interactively (needs DuckDB in PATH)\nmake duckdb\n</code></pre>"},{"location":"getting-started/#install-from-duckdb","title":"Install from DuckDB","text":"<pre><code>install vizier from 'https://cogitatortech.github.io/vizier/extensions';\nload vizier;\n</code></pre> <p>The hosted repository currently includes binaries for DuckDB <code>v1.2.0</code> to <code>v1.5.0</code>.</p> <p>Vizier is currently distributed as an unsigned extension. Start DuckDB with <code>-unsigned</code>, or enable unsigned extensions in your client before installing and loading Vizier.</p>"},{"location":"getting-started/#download-from-the-release-page","title":"Download from the Release Page","text":"<p>Open the latest release for release notes and platform-specific zip downloads:</p> <ul> <li>Linux AMD64: <code>vizier-linux-amd64.zip</code></li> <li>Linux ARM64: <code>vizier-linux-arm64.zip</code></li> <li>Linux AMD64 (musl): <code>vizier-linux-amd64-musl.zip</code></li> <li>Linux ARM64 (musl): <code>vizier-linux-arm64-musl.zip</code></li> <li>macOS ARM64: <code>vizier-macos-arm64.zip</code></li> <li>Windows AMD64: <code>vizier-windows-amd64.zip</code></li> <li>Windows ARM64: <code>vizier-windows-arm64.zip</code></li> <li>FreeBSD AMD64: <code>vizier-freebsd-amd64.zip</code></li> </ul>"},{"location":"getting-started/#loading-the-extension","title":"Loading the Extension","text":"<p>After installing Vizier from the hosted extension repository:</p> <pre><code>install vizier from 'https://cogitatortech.github.io/vizier/extensions';\nload vizier;\n</code></pre>"},{"location":"getting-started/#basic-workflow","title":"Basic Workflow","text":"<p>Vizier follows a capture, analyze, and apply workflow.</p>"},{"location":"getting-started/#1-capture-queries","title":"1. Capture Queries","text":"<p>Feed Vizier the queries you run against your database:</p> <pre><code>select * from vizier_capture('select * from events where account_id = 42 and ts &gt;= date ''2026-01-01''');\nselect * from vizier_capture('select count(*) from orders group by customer_id');\nselect * from vizier_capture('select * from events where account_id = 42 and ts &gt;= date ''2026-01-01''');\n</code></pre> <p>Flush captured queries to metadata tables:</p> <pre><code>select * from vizier_flush();\n</code></pre>"},{"location":"getting-started/#2-review-the-workload","title":"2. Review the Workload","text":"<pre><code>select * from vizier.workload_summary;\n</code></pre> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502     sig      \u2502                                   sql                                    \u2502 runs  \u2502      last_seen      \u2502 avg_ms \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 8c9d7175aab0 \u2502 SELECT * FROM events WHERE account_id = 42 AND ts &gt;= DATE '2026-01-01'   \u2502     2 \u2502 2026-03-22 16:44:06 \u2502    0.0 \u2502\n\u2502 2af899d9dba6 \u2502 SELECT count(*) FROM orders GROUP BY customer_id                         \u2502     1 \u2502 2026-03-22 16:44:06 \u2502    0.0 \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"getting-started/#3-analyze-and-get-recommendations","title":"3. Analyze and Get Recommendations","text":"<pre><code>select * from vizier_analyze();\nselect * from vizier.recommendations;\n</code></pre> <p>Each recommendation includes a kind, target table, SQL to execute, score, and confidence level.</p>"},{"location":"getting-started/#4-apply-a-recommendation","title":"4. Apply a Recommendation","text":"<p>Preview first with a dry run:</p> <pre><code>select * from vizier_apply(1, dry_run =&gt; true);\n</code></pre> <p>Then apply:</p> <pre><code>select * from vizier_apply(1);\n</code></pre>"},{"location":"getting-started/#5-measure-the-impact","title":"5. Measure the Impact","text":"<pre><code>select * from vizier_benchmark('select * from events where account_id = 42', 10);\nselect * from vizier_compare(1);\n</code></pre>"},{"location":"getting-started/#persistent-state","title":"Persistent State","text":"<p>By default, Vizier state is in-memory. To persist across sessions:</p> <pre><code>-- Initialize with a state file\nselect * from vizier_init('/path/to/vizier_state.db');\n\n-- Or configure the path (auto-loads on next extension init)\nselect vizier_configure('state_path', '/path/to/vizier_state.db');\n</code></pre> <p>You can also export and import state between environments:</p> <pre><code>select * from vizier_save('/tmp/vizier_state.db');\nselect * from vizier_load('/tmp/vizier_state.db');\n</code></pre>"},{"location":"getting-started/#bulk-capture","title":"Bulk Capture","text":"<p>If you have a table with query logs:</p> <pre><code>select * from vizier_capture_bulk('query_log', 'sql_text');\nselect * from vizier_flush();\n</code></pre>"},{"location":"getting-started/#session-capture","title":"Session Capture","text":"<p>Capture all queries in a session without calling <code>vizier_capture()</code> per query:</p> <pre><code>select * from vizier_start_capture();\n-- run your queries here\nselect * from vizier_stop_capture();\n</code></pre>"},{"location":"getting-started/#import-from-profiling-output","title":"Import from Profiling Output","text":"<p>If you have a DuckDB JSON profiling file:</p> <pre><code>select * from vizier_import_profile('/path/to/profile.json');\nselect * from vizier_flush();\n</code></pre>"},{"location":"getting-started/#import-from-dbt","title":"Import from dbt","text":"<p>If you use dbt with DuckDB, you can import your dbt run results into Vizier. The import_dbt.py script reads compiled SQL from <code>manifest.json</code> and execution timing from <code>run_results.json</code>:</p> <pre><code># Import all successful model runs\npython scripts/import_dbt.py --db my_database.duckdb\n\n# Only import models that took longer than 0.5 seconds\npython scripts/import_dbt.py --db my_database.duckdb --min-time 0.5\n\n# Preview what would be imported without executing\npython scripts/import_dbt.py --db my_database.duckdb --dry-run\n</code></pre> <p>By default, the script reads from the <code>target/</code> directory. Use <code>--target</code> to point to a different location.</p>"},{"location":"limitations/","title":"Current Limitations","text":"<p>Vizier is in early development. This page documents known limitations, so you can decide whether Vizier fits your use case and interpret its recommendations accordingly.</p>"},{"location":"limitations/#query-capture-is-manual","title":"Query Capture Is Manual","text":"<p>Vizier needs you to explicitly capture queries via <code>vizier_capture()</code>, <code>vizier_capture_bulk()</code>, or session capture. There is no automatic capture that transparently records all queries as they run. This means the quality of recommendations depends entirely on whether you feed Vizier a representative workload.</p> <p>Automatic capture (via DuckDB profiling output or a query log hook) is planned but not yet implemented.</p>"},{"location":"limitations/#sql-parsing-uses-two-strategies","title":"SQL Parsing Uses Two Strategies","text":"<p>Vizier uses two extraction strategies during a flush:</p> <ol> <li> <p>EXPLAIN-based extraction (preferred default): runs <code>EXPLAIN</code> on the captured query using DuckDB's own parser.    This produces accurate table references, filter predicates, and estimated row counts directly from the    query plan. Note that it only works when the referenced tables exist in the current database.</p> </li> <li> <p>Tokenizer-based extraction (fallback): a hand-rolled tokenizer that parses SQL text without    database access. Used when <code>EXPLAIN</code> fails (e.g., tables do not exist, or the query uses unsupported syntax).</p> </li> </ol> <p>The tokenizer fallback has known limitations. It will miss or misparse:</p> <ul> <li>Subqueries and CTEs</li> <li>Window functions with <code>PARTITION BY</code></li> <li>Computed predicates like <code>WHERE f(col) &gt; 0</code></li> <li><code>CASE WHEN</code> expressions in filter positions</li> <li>DuckDB-specific syntax (lambda functions, list comprehensions, and <code>QUALIFY</code>)</li> </ul> <p>The tokenizer also has fixed limits: 1024 tokens and 64 predicates per query. Queries that exceed these limits are silently truncated.</p> <p>For best results, create the tables in your database before capturing and flushing queries so that EXPLAIN-based extraction can run.</p>"},{"location":"limitations/#scoring-is-partially-cost-aware","title":"Scoring Is Partially Cost-Aware","text":"<p>Recommendations are scored using query frequency, predicate counts, and hand-tuned thresholds. When EXPLAIN-based extraction succeeds, the estimated row count from the query plan is factored into scoring (queries scanning more rows get a larger boost from index recommendations).</p> <p>The \"estimated_gain\" values (like \"10-50x faster\") are still generated from templates, not from measured performance. Treat them as rough indicators, not guarantees.</p> <p>Full cost-based scoring that compares optimizer cost before and after a recommendation is not yet implemented.</p>"},{"location":"limitations/#art-indexes-have-limited-value-in-duckdb","title":"ART Indexes Have Limited Value in DuckDB","text":"<p>DuckDB's ART indexes are primarily useful for point lookups and primary key enforcement. The DuckDB optimizer often skips ART indexes for range scans because columnar min/max zone maps already provide pruning. The index advisor may recommend indexes that DuckDB does not use for the queries you care about.</p> <p>Before applying an index recommendation, use <code>vizier_compare(id)</code> to verify the <code>EXPLAIN</code> plan actually uses the index.</p>"},{"location":"limitations/#sort-and-parquet-advisors-do-not-reason-about-tradeoffs","title":"Sort and Parquet Advisors Do Not Reason about Tradeoffs","text":"<p>When different queries benefit from different sort orders (like some filtering by <code>account_id</code> and others by <code>region</code>), the sort and Parquet advisors pick the most frequent pattern. They do not report the tradeoff or warn that other queries may regress.</p> <p>Use <code>vizier_replay()</code> after applying a sort recommendation to check for regressions across the full workload.</p>"},{"location":"limitations/#best-suited-for-persistent-repeated-workloads","title":"Best Suited for Persistent, Repeated Workloads","text":"<p>Vizier works best when queries repeat over time against a persistent DuckDB database. It is less useful for:</p> <ul> <li>Ad-hoc analytics where queries change constantly</li> <li>Read-only Parquet-over-S3 scans where you cannot add indexes or rewrite tables</li> <li>ETL pipelines where the engineer already knows the access patterns</li> </ul> <p>If your workload does not have repeating patterns, Vizier's frequency-based scoring will not produce meaningful recommendations.</p>"}]}